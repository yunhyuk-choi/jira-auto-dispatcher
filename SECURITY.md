# SECURITY.md — 이 소프트웨어를 실행하면 무엇에 동의하는가 + 보안 태세

> **이 문서는 두 가지다.** (1) 이 소프트웨어를 기동하는 행위가 **무엇에 대한 동의인지**를
> 설치자에게 알리는 **선언문**, (2) 그 위험을 어떻게 감싸고 있는지의 **보안 태세 기술**.
>
> 이 프로젝트는 사람의 매 단계 승인 없이 도구 권한을 가진 코딩 에이전트를 헤드리스로
> 실행한다. 그건 버그도, 나중에 고칠 임시 조치도 아니라 **설계의 전제**다. 그러므로
> "쓰다 보니 그런 줄 몰랐다"가 성립하지 않도록 여기서 먼저 명시한다.

---

## 1. 동의 선언 — 실행 = 풀 퍼미션 부여

**이 소프트웨어를 기동하면, 당신은 자율 에이전트에게 풀 퍼미션을 부여하는 데 동의한
것이다.** 구체적으로 무엇에 동의하는가:

| 당신이 붙이는 것 | 에이전트가 그것으로 하는 일 |
|---|---|
| **forge 토큰**(GitLab/GitHub PAT) | 토큰 권한 범위의 레포에 브랜치를 만들고 **push**, MR/PR 생성·수정 |
| **Jira 토큰** | 티켓 조회·상태 전이·코멘트 작성 (그 사용자 이름으로) |
| **Claude 토큰**(`claude setup-token`) | 당신의 구독 사용량으로 LLM 호출 |
| **워커 컨테이너** | 그 안에서 **임의의 셸 명령·파일 쓰기** 실행 (승인 프롬프트 없음) |
| **docker 소켓(또는 socket-proxy)** | central 이 컨테이너를 생성·기동·exec — 실효 권한은 **호스트 root 동치**(§4) |

worker 컨테이너의 `claude` 는 `--dangerously-skip-permissions` 를 **헤드리스**로 실행한다.
대화형 승인 다이얼로그를 사람이 눌러줄 수 없으므로, 스포너(`app/spawner.py`)가 컨테이너
생성 시 그 사용자 `~/.claude/settings.json`(`CLAUDE_CONFIG_DIR`)에 **사전 인가 설정**을
써 넣는다:

```json
{
  "permissions": { "defaultMode": "bypassPermissions" },
  "skipDangerousModePermissionPrompt": true,
  "skipAutoPermissionPrompt": true,
  "skipWorkflowUsageWarning": true
}
```

- 이는 **시스템 레벨 인가**다 — 모든 worker 컨테이너에 공통 적용되며, **사용자 입력이
  필요 없다**. 온보딩은 자격증명만 받고 **권한 승인 단계는 없다**.
- 이 파일은 read-only 바인드로 주입되어 컨테이너 런타임이 되돌릴 수 없다.

### 1.1 동의를 설정에 남긴다

`config.yaml` 의 `consent.full_permissions` 를 **명시적으로 `true`** 로 두어야 한다(기본
`false`). `consent.accepted_at` 에 동의 시각을 남기면 감사 흔적이 된다.

```yaml
consent:
  full_permissions: true
  accepted_at: "2026-01-01T09:00:00+09:00"
```

> 기존 배포 호환을 위해 지금은 미동의 상태여도 부팅은 되고 경고만 남는다. 그러나 **미동의로
> 운영하지 말라** — 이 값은 "읽고 이해했다"의 유일한 기록이다.

## 2. 왜 권한을 죽일 수 없는가 (구조적 이유)

- **헤드리스 자율이 목적이다.** 이 시스템의 존재 이유는 신규 Jira 티켓에서 사람 개입 없이
  worker 오케스트레이터를 기동해 레포 작업을 수행하는 것이다. 대화형 승인 루프는 자율
  실행과 원천적으로 양립하지 않는다 — 눌러줄 사람이 없다.
- 따라서 권한을 "죽여" 대화형으로 되돌리는 것은 선택지가 아니며, 대신 **실행 표면을
  격리·가역·조임으로 감싸는 보완 통제**로 위험을 관리한다(§3).
- 그 결과, **이 시스템의 안전은 권한 축소가 아니라 "노출 축소 + 가역성"에 걸려 있다.**
  아래 §3.0 을 어기면 나머지 통제는 의미가 없다.

## 3. 보완 통제 (위험 관리의 실체)

### 3.0 가장 중요한 통제 — 노출 금지 (설치자 책임)

| 통제 | 내용 |
|---|---|
| **인터넷 노출 금지** | 관리 UI(8787)·worker 는 **RCE 표면**이다. 퍼블릭 IP 바인딩·포트포워딩·공개 리버스 프록시 금지. |
| **신뢰 네트워크 한정** | 사설망/VPN 안에서만. 클라우드 VM 이면 **보안그룹 인바운드를 전부 차단**하고 **SSH 터널**로 접근한다([INSTALL.md](INSTALL.md) 갈래 B). |
| **전용 호스트** | 실효 권한이 호스트 root 동치이므로(§4) 다른 워크로드와 호스트를 공유하지 않는다. |
| **토큰 최소권한** | forge/Jira 토큰은 전용 계정 + 필요한 레포·프로젝트로 스코프를 좁힌다. |
| **웹훅만 예외** | 인바운드가 꼭 필요하면 리버스 프록시(TLS)로 `webhook.path` **하나만** 노출하고 출처 IP 를 제한한다. 8787 관리 UI 는 절대 함께 열지 않는다. |

### 3.1 코드가 강제하는 통제

| 통제 | 내용 | 위치 |
|---|---|---|
| **MR 게이트 · 자동머지 없음** | 산출은 브랜치/MR **초안**까지. 병합은 사람이 한다. | 설계 계약 |
| **가역성** | 산출은 항상 가역 단위(브랜치 `auto/<TICKET>`·MR). 사고 시 브랜치 삭제로 리셋. | `app/agent_runner.py`·`config.git.branch_prefix` |
| **dedup 게이트** | 폴러·웹훅을 수렴해 중복 트리거 제거(무한루프 방지). | `app/gate.py` |
| **트리거=작업 티켓** | worker 는 새 사이클 티켓을 만들지 않는다(자기되먹임 루프 차단). | 설계 계약 |
| **동시성/레포락** | 레포 단위 배타락(같은 레포 병렬 금지 → 교차오염 차단). | `app/scheduler.py` |
| **자원 어드미션** | 잡 수 cap 이 아니라 호스트 메모리·부하로 dispatch 를 조절(폭주 완충). | `app/scheduler.py`·`config.admission` |
| **컨테이너 격리** | 사용자마다 별도 컨테이너. `mem_limit`(기본 4g)는 하드 백스톱. | `app/spawner.py` |
| **비-root 실행** | worker 컨테이너는 `user=1000:1000`(`spawn.run_as`)로 실행 — 특권 축소. | `app/spawner.py` |
| **시크릿 볼륨(ro)** | 토큰은 값이 아니라 per-user 시크릿 디렉토리를 **read-only** 마운트로 노출. 다른 사용자 시크릿은 안 보인다. Jira/forge 는 값 대신 **파일경로**로 주입. | `app/spawner.py` |
| **시크릿 미노출** | 시크릿 값은 `config.read_secret` 로만 읽고, env dict·예외·로그에 절대 싣지 않는다. 온보딩 응답에도 토큰 값 없음. | `app/onboarding.py`·`app/spawner.py` |
| **서비스 토큰 격리** | central **서비스 자신**의 토큰은 worker env 에서 상속분까지 제거 — 산출물이 서비스 계정으로 오염되지 않는다. | `app/agent_runner.py` |
| **안전 기본 enabled=false** | 온보딩 사용자는 비활성으로 시작. 운영자가 검토 후 명시적으로 활성화. | `app/onboarding.py` |
| **향후 조임** | `permission_level` 로 사용자별 권한을 조일 수 있다(현재 `bypass` 만; `sandbox`·`allowlist` 는 TODO 분기). | `app/spawner.py` |

## 4. docker.sock 특권 (별도 위험)

central 의 스포너가 `docker.sock` 에 접근해 컨테이너를 띄우는 것은 사실상 **호스트 root
권한과 동치**다(특권 상승 표면). 완화책:

- **docker-socket-proxy**(tecnativa 등)를 앞단에 두어 `CONTAINERS`/`POST` 만 최소 허용하고,
  central 은 프록시 TCP 엔드포인트로만 접근한다(`deploy.docker_host=tcp://socket-proxy:2375`).
  sock 직결은 개인 로컬 평가 한정.
- **central 자체를 비-root** 로 실행, 신뢰 네트워크 한정.

### 4.1 socket-proxy EXEC 허용 (프랙탈 PUSH — 명시적 결정)

프랙탈 PUSH 경로는 central 역할의 per-user sub 가 이미 살아있는 워커 세션에
`docker exec jad-worker-<user> claude -p --resume ...` 로 이어붙이는 방식이다.
이를 위해 socket-proxy 의 `EXEC` 를 **명시적으로 1로 켰다**(기본 0 → 1,
`docker-compose.yml` socket-proxy `environment`). central 엔 원격 프록시를 타깃하는
docker CLI(클라이언트 전용, `DOCKER_HOST=tcp://socket-proxy:2375`)를 함께 넣었다.

- **트레이드오프(정직하게).** 프록시의 `EXEC` 는 리소스 범위를 구분하지 못한다 —
  프록시가 가리키는 대상은 **호스트 데몬**이므로, exec 권한은 `jad-worker-*` 뿐 아니라
  **호스트의 임의 컨테이너**에 exec 를 붙일 수 있다. 다만 이미 `CONTAINERS`+`POST`
  (컨테이너 생성·기동)를 허용한 시점에서 실효 권한은 **호스트 root 동치**였다(§4).
  따라서 EXEC 은 **새로운 상한을 만들지 않는다** — 이미 존재하던 상한에 대한
  *편의/도달 용이성*을 더할 뿐이다. 그럼에도 공격 표면이 넓어지고 오용 가능성이
  올라가는 것은 사실이므로 여기 명시해 둔다.
- **호스트 선택의 의미.** 실효 권한이 호스트 root 동치라는 것은, **이 시스템을 다른
  워크로드와 같은 호스트에 두면 안 된다**는 뜻이다. 전용 VM/서버를 권한다.
- **권장 향후 조임(기록).** central 의 docker 접근을 자기 워커로만 가두려면
  **중첩(inner) docker 데몬**을 도입하는 것이 정공법이다 — 예: **Sysbox** 런타임으로
  central 을 시스템 컨테이너로 띄우고 그 안에 전용 docker 데몬을 두면, central 의
  create/start/exec 가 **그 내부 데몬의 컨테이너(=자기 워커)로만** 미치고 호스트
  데몬엔 닿지 않는다. 이때 socket-proxy·EXEC 노출은 제거할 수 있다. (현 단계에선
  미도입 — 신뢰 네트워크 한정·MR 게이트·비-root·가역성의 기존 다층 통제로 관리한다.)

## 5. permission_level 로 조이기

레지스트리 사용자 레코드의 `permission_level`(기본 `bypass`)로 사전 인가 강도를
사용자별로 조정할 수 있다. 현재는 `bypass` 만 구현돼 있고, `sandbox`(격리 FS/네트워크)·
`allowlist`(도구/명령 화이트리스트)는 `app/spawner.py` 의 `render_settings()` 에 **TODO 분기
자리**로 예약돼 있다. 권한을 더 조이려면 이 분기를 채우고 사용자 `permission_level` 을
변경하면 된다.

## 6. 취약점 신고

이 저장소에서 보안 결함을 발견하면 공개 이슈로 올리기 전에 저장소 관리자에게 비공개로
알려 주기 바란다. 다만 **§1 에 기술된 동작(승인 없는 도구 실행, 헤드리스 bypass)은 취약점이
아니라 설계된 동작**이다 — 그 전제 위에서 벗어나는 결함(예: 시크릿 유출, 사용자 간 격리
붕괴, 의도치 않은 권한 상승, 노출 통제 우회)을 대상으로 한다.

---

**요약:** 자율 헤드리스 실행에는 대화형 승인이 성립하지 않으므로 이 시스템은 bypass 사전
인가를 **설계 전제로 채택**한다. 그 위험은 **노출 축소**(신뢰 네트워크 한정·인터넷 노출
금지·전용 호스트)와 **가역성**(MR 게이트·결정적 브랜치)을 축으로, dedup·레포락·자원
어드미션·컨테이너 격리·비-root·시크릿 ro 볼륨·시크릿 미노출·`permission_level` 조임 여지의
다층 통제로 관리한다. **이 소프트웨어를 켜는 것은 그 전부를 이해하고 감수한다는 뜻이다.**
